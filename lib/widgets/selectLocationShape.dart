import 'package:flutter/material.dart';
class SelectLocationShape extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('sssssssssssssss'),
   Divider(
     height: 2,
     color: Colors.grey,
     thickness: 2,
   )
      ],
    );
  }
}
